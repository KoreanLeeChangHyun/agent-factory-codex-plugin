# Follow-up Inquiry: bounded loop hardening design

Continue the same loop-engineering Inquiry. Convert the previously identified risks into an implementable, backward-conscious hardening design for this repository.

Prioritize:

1. Prevent false completion from pre-Work or unbound test evidence while preserving Main/Human-only test authority.
2. Bind accepted evidence to the exact original request, latest Work run, and a reproducible snapshot of the reviewed workspace; define the smallest standard-library/git implementation and mutation checks.
3. Add exact dispatch idempotency/correlation across the pending-dispatch ACK crash window without replaying a started turn.
4. Enforce follow-up Review finding lifecycle and versioned loop-state invariants.
5. Define bounded result/receipt/log provenance and process-tree cancellation suitable for Linux while preserving portability failure behavior.

Inspect primary durable-workflow, provenance, and agent-orchestration sources where useful, including OpenAI Symphony and official platform/runtime documentation. Identify which changes can be made without a Human product choice and which must remain deferred.

Do not implement product changes or run tests. Append a concrete phased design, proposed CLI/state schemas, compatibility decisions, threat cases, and acceptance tests to `.agent-factory/inquery/loop-engineering-2026-08/notes.md`. Publish the concise result through the managed result path.
