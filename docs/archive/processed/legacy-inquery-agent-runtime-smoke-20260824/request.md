# Agent runtime smoke test

Question: Does the managed Agent Factory Inquiry Agent complete a minimal read-only repository inspection successfully?

Scope:

- Read `AGENTS.md`, `skills/agent/SKILL.md`, and the role references that the Agent contract requires.
- Identify the four supported Agent roles and give one sentence describing each role's responsibility.
- Do not modify repository product files.
- Write brief unrefined Markdown notes only inside `.agent-factory/inquery/agent-runtime-smoke-20260824/`.
- Do not run tests, builds, linters, or type checks.

Completion condition: Publish an evidence-backed result through the runtime-declared result path, including the inspected source paths, the four roles, limitations, and whether the requested read-only inspection completed.
