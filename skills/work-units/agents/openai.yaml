interface:
  display_name: "Agent Factory Work Units"
  short_description: "Run optional advanced execution artifacts."
  default_prompt: "Use $work-units only for an explicitly selected Work Unit, Work Package, or linked worktree route."
policy:
  allow_implicit_invocation: false
