interface:
  display_name: "Agent Factory Work Unit Planner"
  short_description: "Create executable Work Units from ready Intake basis."
  default_prompt: "Use $work-unit-planner to manage and validate Work Units only through assets/scripts/work_unit.py; fail closed instead of editing canonical JSON directly."
policy:
  allow_implicit_invocation: true
