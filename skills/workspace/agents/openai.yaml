interface:
  display_name: "Agent Factory Workspace"
  short_description: "Manage Agents, documents, and project views"
  default_prompt: "Use $workspace to create or maintain the Human-facing Agent Factory control tower."
policy:
  allow_implicit_invocation: true
