interface:
  display_name: "Agent Factory Specification"
  short_description: "Align versioned Specification profiles and reports."
  default_prompt: "Use $specifications to manage Specifications and diagrams, writing canonical JSON only through scripts/specification.py."
policy:
  allow_implicit_invocation: true
