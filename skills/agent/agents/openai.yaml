interface:
  display_name: "Agent Factory Agent"
  short_description: "Apply the captured Agent execution mode."
  default_prompt: "Use $agent to follow the captured task mode and its completion rules, preserve the independent Human approval gate, and let Main perform only authorized Git actions under Convention's publication contract."
