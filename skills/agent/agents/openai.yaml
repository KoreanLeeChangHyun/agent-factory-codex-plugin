interface:
  display_name: "Agent Factory Agent"
  short_description: "Run the Main, Work, and Verification graph."
  default_prompt: "Use $agent to route work through Main -> Work -> Verification, repeat on failure, and let Main directly commit the exact result after pass or evidenced Human skip."
