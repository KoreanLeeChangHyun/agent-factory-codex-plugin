interface:
  display_name: "Agent Factory Agent"
  short_description: "Orchestrate Main and bounded managed Exec roles."
  default_prompt: "Use $agent for a Human-facing Interview or to route research, implementation, review, and authorized verification through the appropriate managed roles."
