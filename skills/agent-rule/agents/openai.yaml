interface:
  display_name: "Agent Rule"
  short_description: "Apply Agent Factory edit, evidence, and engineering rules."
  default_prompt: "Use $agent-rule before editing or designing Agent Factory work so the change is explicit, evidence-backed, and scoped."
policy:
  allow_implicit_invocation: true
