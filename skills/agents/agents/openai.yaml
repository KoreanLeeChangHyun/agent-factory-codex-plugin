interface:
  display_name: "Agent Factory Agents"
  short_description: "Manage resumable background exec agents."
  default_prompt: "Use $agents to run the Main Agent or manage addressable background Inquiry Agent sessions through the file-based exec runtime."
